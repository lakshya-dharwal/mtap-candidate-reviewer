# Pinned to the versions this project is developed/verified against.
# Unpinned deps already broke this build twice mid-project (matplotlib 3.11
# renamed boxplot's `labels` kwarg; streamlit deprecated use_container_width) —
# pin, don't re-learn that lesson.
bravado==12.0.1
bravado-core==6.4.1
pandas==3.0.3
numpy==2.4.6
scikit-learn==1.9.0
xgboost==3.2.0
shap==0.51.0
matplotlib==3.11.0
scipy==1.17.1
streamlit==1.59.0
requests==2.34.2
joblib==1.5.3
pytest==8.4.1
